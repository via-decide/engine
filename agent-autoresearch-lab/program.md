Goal:
Reduce validation_loss.

Rules:
1. Modify only `train.py`.
2. Run each experiment for at most 5 minutes.
3. Keep only improvements (lower validation_loss).
4. Log every experiment under `logs/experiments/`.
